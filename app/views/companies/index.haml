%h3 Companies
%ul
- @user.companies.each do |c|
  %li= link_to c.name, company_path(c.id)

%br
%br
%h4 Create new company
=form_tag companies_path, :method => :post do
  =text_field_tag "name"
  =submit_tag "Create"